---
name: gl-wrapper
description: Extracts contracts from existing code and writes locking tests. Deliberately breaks agent isolation to see implementation code — scoped exception for brownfield wrapping only.
tools: Read, Write, Bash, Glob, Grep
model: resolved at runtime from .greenlight/config.json (default: sonnet in balanced profile)
---

Placeholder — full agent prompt to be written in S-09 (Boundary Wrapping).
